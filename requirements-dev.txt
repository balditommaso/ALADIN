# Packages you're experimenting with, not yet promoted to the "official"
# .devcontainer/requirements.txt baked into the image.
#
# Installed automatically:
#   - once, when the container is created / rebuilt (postCreateCommand)
#   - again, every time you reopen/start the container (postStartCommand)
#
# No image rebuild needed — just edit this file and run:
#   Dev Containers: Rebuild Container   (to be safe)
# or simply reopen the container to pick it up via postStartCommand.
#
# Example:
# einops==0.7.0
argcomplete
pyelftools